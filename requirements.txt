# LLM OS - 필수 패키지만 (간단 버전)

# 웹 프레임워크
streamlit>=1.28.0

# AI 라이브러리 (실제 작동하는 버전)
openai>=1.3.0
anthropic>=0.7.0  
google-generativeai  # 버전 제한 제거 (자동으로 호환 버전 설치)

# 이미지 처리
Pillow>=10.0.0

# 환경 변수 관리
python-dotenv>=1.0.0

# HTTP 요청 (대부분 라이브러리에 포함되어 있음)
requests>=2.31.0

# 기타 필수
typing-extensions>=4.0.0